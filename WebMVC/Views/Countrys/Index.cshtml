
@model IEnumerable<Domain.Entities.Country>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CountryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CountryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Addresses)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <th>
                @Html.DisplayFor(modelItem => item.CountryId)
            </th>
            <th>
                @Html.DisplayFor(modelItem => item.CountryName)
            </th>
            <th>
                @Html.DisplayFor(modelItem => item.Addresses)
            </th>
            <td>
                <a asp-action="Edit" asp-route-CountryId="@item.CountryId">Edit</a> |
                <a asp-action="Details" asp-route-CountryId="@item.CountryId">Details</a> |
                <a asp-action="Delete" asp-route-CountryId="@item.CountryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
